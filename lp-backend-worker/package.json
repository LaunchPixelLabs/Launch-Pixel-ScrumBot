{
  "name": "lp-backend-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker serving as active backend and webhook listener for Launch Pixel Scrum",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
