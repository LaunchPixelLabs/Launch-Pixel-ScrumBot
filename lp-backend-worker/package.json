{
  "name": "lp-bot",
  "version": "2.0.0",
  "description": "LP_Bot — LaunchPixel's 24/7 AI Scrum Master on Cloudflare Workers (Discord interactions + dual-brain council + Neon memory).",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "register": "node scripts/register-commands.mjs",
    "logs": "wrangler tail"
  },
  "license": "ISC",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "discord-interactions": "^4.4.0",
    "hono": "^4.12.28"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "wrangler": "^4.81.0"
  }
}
